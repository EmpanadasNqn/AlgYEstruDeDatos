#include <stdio.h>
#include <limits.h> //Define valores maximos y minimos de los tipos basicos de C. Ejemplo cuando necesito un -Infinito o +Infinito

int main(){
    printf("El valor minimo de Int es %d\n",INT_MIN);
    printf("El valor maximo de Int es %d\n",INT_MAX);
    
    return 0;
}